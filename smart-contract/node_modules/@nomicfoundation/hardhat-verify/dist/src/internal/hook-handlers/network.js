export default async () => ({
    async newConnection(context, next) {
        const connection = await next(context);
        const { Verification } = await import("../verification-helpers.js");
        connection.verification = new Verification(connection.provider, connection.networkName, context.config.chainDescriptors, context.config.verify);
        return connection;
    },
});
//# sourceMappingURL=network.js.map